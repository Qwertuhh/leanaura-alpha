{
  "name": "api",
  "version": "1.0.0",
  "repository": "https://github.com/qwertuhh/toci-alpha",
  "scripts": {
    "dev": "uv run serve --dev",
    "build": "uv run build",
    "start": "uv run serve"
  }
}
